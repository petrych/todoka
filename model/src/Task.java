/** * A task has a name and a checklist. * A task can be created and saved (from any list view - either time list or category list), * edited and saved, removed, completed. * A task is stored always in one time list (mandatory; Today by default). * May also be listed in one (only one) category. */import java.util.ArrayList;public class Task {    private String name;    private ArrayList<String> checklist;    private String timeList = "TODAY";    private String category = "<empty>";    //TODO - main method is here for testing purposes    public static void main(String[] args) {        Task aTask = new Task();        System.out.println("Name: " + aTask.getName() + ", timeList is: " + aTask.getTimeList() +                            ", category is: " + aTask.getCategory());    }    // create a task when name field is empty and no other details are provided    public Task() {        this.name = "?";    }    // TODO - how to pass a cheklist item?    public Task(String name, ArrayList<String> checklist) {        this.name = name;        this.checklist = checklist;    }    public Task(ArrayList<String> checklist) {        new Task();        this.checklist = checklist;    }    public String getName() {        return name;    }    public void setName(String name) {        this.name = name;    }    public String getTimeList() {        return timeList;    }    public void setTimeList(String timeList) {        this.timeList = timeList;    }    public String getCategory() {        return category;    }    public void setCategory(String category) {        this.category = category;    }}